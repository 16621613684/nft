CREATE TABLE auction (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         goodId INT,
                         endTime DATETIME,
                         startingPrice DOUBLE,
                         currentPrice DOUBLE,
                         highestBidderId INT,
                         status INT
);
